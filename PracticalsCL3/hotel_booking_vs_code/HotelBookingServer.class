// Source code is decompiled from a .class file using FernFlower decompiler.
import java.rmi.Naming;
import java.rmi.registry.LocateRegistry;

public class HotelBookingServer {
   public HotelBookingServer() {
   }

   public static void main(String[] var0) {
      try {
         LocateRegistry.createRegistry(1099);
         System.out.println("RMI registry started on port 1099.");
         HotelBookingImpl var1 = new HotelBookingImpl();
         Naming.rebind("HotelBookingService", var1);
         System.out.println("Hotel Booking Service is ready.");
      } catch (Exception var2) {
         System.err.println("Server exception: " + var2.toString());
         var2.printStackTrace();
      }

   }
}
